// Design-only mock counts shared by MedicalCentreShell's nav badges and the
// dashboard page — kept in one place so both stay in sync until these are
// replaced by real OPD/sick-room/pharmacy data.
export const MEDICAL_CENTRE_MOCK_SUMMARY = {
  opdWaiting: 4,
  bedsOccupied: 2,
  lowStockCount: 3,
};
