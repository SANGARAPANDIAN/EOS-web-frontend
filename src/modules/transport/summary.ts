// Design-only mock counts shared by TransportShell's nav badges and the
// dashboard page — kept in one place so both stay in sync until these are
// replaced by real fleet/crew/compliance data.
export const TRANSPORT_MOCK_SUMMARY = {
  busesCount: 12,
  routesCount: 12,
  crewCount: 24,
  maintenanceDueCount: 1,
  complianceExpiringCount: 2,
};
